# verif-frames-para-pdl-elixir
Implementação de um verificador de frames para programas PDL, feito em Elixir.

### Para rodar
Navege até a raiz do projeto '/pdl' e execute:
```
mix run lib/pdl.ex input.txt
```